targets:
  $default:
    sources:
      - lib/**
      - resources/schema/schema.graphql
      - resources/queries/query.graphql
    builders:
      artemis:
        options:
          schema_mapping:
            - schema: resources/schema/schema.graphql
              queries_glob: resources/queries/query.graphql
              output: lib/api/api.query.dart
